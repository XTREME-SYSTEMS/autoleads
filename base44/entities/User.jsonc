{
  "properties": {
    "role": {
      "type": "string",
      "description": "The role of the user in the app",
      "enum": [
        "admin",
        "user"
      ]
    },
    "organization_ids": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "description": "IDs of organizations this user belongs to (denormalized from OrganizationMembership for RLS)"
    },
    "active_organization_id": {
      "type": "string",
      "description": "Currently selected working organization"
    }
  },
  "required": [
    "role"
  ],
  "name": "User",
  "type": "object"
}