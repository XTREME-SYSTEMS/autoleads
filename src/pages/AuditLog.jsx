import React from "react";
import ModuleShell from "./ModuleShell";
export default function AuditLog() { return <ModuleShell title='Audit and Receipts' description='Review tenant-scoped actions, agent runs, approval decisions, external calls, failures, and rollback metadata.' />; }